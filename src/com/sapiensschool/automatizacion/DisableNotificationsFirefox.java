package com.sapiensschool.automatizacion;

import org.openqa.selenium.By;
import org.openqa.selenium.firefox.FirefoxProfile;

public class DisableNotificationsFirefox {

	public static void main(String[] args) {
		// Running it with Firefox
		/* FirefoxProfile ffprofile = new FirefoxProfile();
		ffprofile.setPreference("dom.webnotifications.enabled", false);
		WebDriver driver = new FirefoxDriver(ffprofile);
		customDriver.get("https://www.reddit.com/");
		customDriver.findElement(By.id("header-search-bar")).sendKeys("TestingConcepts");
		customDriver.close(); */
		
		/* The request was done on September 2020
		 * We faced many challenges to finish it
		 */

	}

}
